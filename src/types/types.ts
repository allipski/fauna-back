export type IndividualsFilter = {
    species?: number, 
    project?: number, 
    speciesName?: string, 
    gender?: string, 
    location?:string, 
    health?: string, 
    rehab?: boolean, 
    released?: boolean
};